version: '3.8'

services:
  hello-app:
    image: nginxdemos/hello:plain-text
    container_name: hello-world-container
    restart: unless-stopped
    expose:
      - '80'
    networks:
      - web-gateway-net

networks:
  web-gateway-net:
    external: true
    name: web-gateway
